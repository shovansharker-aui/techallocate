// Copy this file to ai_config.dart (already git-ignored, so your key is
// never committed) and paste in your own FREE Gemini API key, generated
// with no credit card at https://aistudio.google.com/apikey.
//
// Leave geminiApiKey empty (or don't create ai_config.dart at all) to
// disable "Others" task AI titles entirely -- summarization then just
// silently no-ops and the app behaves exactly as before.
const geminiApiKey = '';
